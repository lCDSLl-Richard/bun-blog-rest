{
  "name": "todo-app",
  "version": "1.0.50",
  "scripts": {
    "start": "bun run build/bundle.js || echo \"no build found!\"",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "bun run --watch src/index.ts",
    "build": "bun build src/index.ts --outfile build/bundle.js --minify"
  },
  "dependencies": {
    "elysia": "latest"
  },
  "devDependencies": {
    "bun-types": "latest"
  },
  "module": "src/index.js"
}
